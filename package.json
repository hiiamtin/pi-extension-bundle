{
  "name": "@tintin/pi-extensions",
  "version": "0.5.0",
  "description": "pi extensions for the TinTin setup: AI quota status + web search + web fetch + background tasks (run/status/log/wait/artifact/kill, log watches, auto-bg interceptor, per-session ownership)",
  "keywords": [
    "pi-package"
  ],
  "license": "UNLICENSED",
  "private": true,
  "pi": {
    "extensions": [
      "./extensions"
    ]
  }
}
